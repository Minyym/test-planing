---
phase: 01-duplex-realtime-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - packages/protocol/package.json
  - packages/protocol/src/control.ts
  - packages/protocol/src/audio.ts
  - packages/protocol/src/render.ts
  - packages/protocol/src/index.ts
  - packages/protocol/tests/control.spec.ts
  - packages/protocol/tests/render.spec.ts
autonomous: true
requirements:
  - CTRL-02
user_setup:
  - "Provide NPM token if publishing @alex/protocol externally (optional for local workspace)"
must_haves:
  truths:
    - All duplex payloads share a versioned zod schema validated in CI before publish
    - Browser and gateway can import the same TypeScript types without TS path hacks
  artifacts:
    - packages/protocol/src/index.ts exporting AudioHeaderSchema/RenderEnvelopeSchema/ControlFrameSchema
    - pnpm-workspace.yaml + root tsconfig enabling shared package resolution
  key_links:
    - Gateway plan depends on protocol package version declared via pnpm workspace reference
---

<objective>
打造 `@alex/protocol` 工作区包，提供双通道 WS header/control schema、编码/解码工具与测试，使后续 gateway/客户端无需再次定义协议即可编译并通过类型检查。

Purpose: 解决 CTRL-02 要求的“Duplex WebSocket 协议以 zod schema 定义并共享给前后端”。  
Output: pnpm workspace、protocol 包源码、单元测试、文档注释。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-duplex-realtime-backbone/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 pnpm workspace 与 protocol 基础结构</name>
  <files>pnpm-workspace.yaml, package.json, tsconfig.base.json, packages/protocol/package.json, packages/protocol/tsconfig.json</files>
  <action>
    - 初始化 pnpm workspace（root package.json: private true, workspaces 指向 packages/*, apps/*）。
    - 写 `pnpm-workspace.yaml` 与 `tsconfig.base.json`，开启 path alias `@alex/*`。
    - 在 `packages/protocol` 创建 package.json（module=ESM, types 指向 dist, scripts: build, lint, test）。
    - 配置 `tsup` or `tsc` build（首选 `tsup`）+ `vitest` 测试脚本，`exports` 显式列出 `./audio`, `./control`, `./render`。
    - 在 README 记录版本策略（semver + protocolVersion 字段）。
    - WHY: 没有工作区标准，后续 gateway/cient 引用将乱套；使用 pnpm workspace 可保持单版本 source-of-truth。
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter @alex/protocol build</automated>
  </verify>
  <done>
    - pnpm workspace 可在本地安装依赖，protocol 包成功构建。
    - `package.json#exports` 包含 audio/render/control 子入口。
  </done>
</task>

<task type="auto">
  <name>Task 2: 定义 zod schema + 编码工具 + 单测</name>
  <files>packages/protocol/src/audio.ts, packages/protocol/src/render.ts, packages/protocol/src/control.ts, packages/protocol/src/index.ts, packages/protocol/tests/*.spec.ts</files>
  <action>
    - 依据研究稿定义 `AudioHeaderSchema`（sessionId, seq, format enum, ts monotonic, chunkDurationMs）、`RenderEnvelopeSchema`（token, viseme[], citeRefs[], latencyMs）、`ControlFrameSchema`（interrupt/start/resume 枚举 + ackId）。
    - 提供 `encodeBinaryHeader(DataView)` & `decodeBinaryHeader`，并导出 K/V 常量 `PROTOCOL_VERSION`。
    - 写 Vitest 覆盖：
      1. schema 校验非法字段报错；
      2. encode→decode 保持字段一致；
      3. `ControlFrameSchema.safeParse` 在 missing ackId 时失败。
    - 生成 `d.ts` 并 re-export 在 `src/index.ts`，包含 JSDoc 说明打断流程。
    - WHY: 缺少 schema/测试无法证明协议满足 CTRL-02；需要 binary header 编码以串起浏览器和 gateway。
  </action>
  <verify>
    <automated>pnpm --filter @alex/protocol test --runInBand</automated>
  </verify>
  <done>
    - 所有 schema/工具通过单元测试。
    - 协议版本常量存在且被导出供 Gateway 使用。
  </done>
</task>

</tasks>

<verification>
- [ ] pnpm --filter @alex/protocol lint && pnpm --filter @alex/protocol test
- [ ] pnpm --filter @alex/protocol build 生成 dist/*
- [ ] README 记录协议版本 + channel 描述
</verification>

<success_criteria>

- 所有 schema 编译并有单元测试保护。
- Gateway/客户端能共用 `@alex/protocol` 类型。
- CTRL-02 协议共享目标具备落地基础。
  </success_criteria>

<output>
执行完成后生成 `.planning/phases/01-duplex-realtime-backbone/01-01-SUMMARY.md` 记录成果。
</output>
