---
phase: 01-duplex-realtime-backbone
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - apps/gateway/src/telemetry/tracer.ts
  - apps/gateway/src/metrics/histograms.ts
  - apps/gateway/src/server.ts
  - tests/load/k6-duplex.js
  - observability/dashboards/ctrl-latency.json
  - docs/observability/CTRL-03.md
autonomous: true
requirements:
  - CTRL-03
user_setup:
  - "Provision Honeycomb/Grafana credentials + API keys"
  - "Install k6 locally (pnpm dlx k6) or provide docker image"
must_haves:
  truths:
    - Interrupt latency、ASR/TTS latency、RAG 命中率均作为 OpenTelemetry metrics 暴露
    - k6 压测脚本能在 5 min 内重现 dashboard 指标
  artifacts:
    - apps/gateway/src/telemetry/tracer.ts 配置 OTLP exporter + span hooks
    - observability/dashboards/ctrl-latency.json 包含 3 个图：Interrupt ACK、ASR/TTS latency、RAG hit rate
    - tests/load/k6-duplex.js 脚本发送音频帧 + interrupt + 校验响应
  key_links:
    - Telemetry 代码写入 docs/observability/CTRL-03.md 操作指南
    - k6 输出 JSON 作为 dashboards 数据源校验
---

<objective>
补齐 CTRL-03：构建端到端可观测性（OpenTelemetry + Grafana/Honeycomb）与 k6 压测脚本，持续验证打断、ASR/TTS 延迟与 RAG 命中率。

Purpose: 没有指标就无法证明 Phase 1 成功。  
Output: telemetry 模块、仪表板 JSON、运行手册、k6 测试脚本。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-duplex-realtime-backbone/01-RESEARCH.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/phases/01-duplex-realtime-backbone/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 注入 OpenTelemetry + metrics 导出</name>
  <files>apps/gateway/src/telemetry/tracer.ts, apps/gateway/src/metrics/histograms.ts, apps/gateway/src/server.ts</files>
  <action>
    - 新建 `telemetry/tracer.ts` 注册 `NodeSDK` + OTLP exporter（env: OTEL_EXPORTER_OTLP_ENDPOINT, API key）。
    - 定义 metrics: `duplex_interrupt_ack_ms`, `asr_latency_ms`, `tts_latency_ms`, `rag_hit_ratio` (gauge via upDownCounter)。
    - 在 `server.ts` hooking：
      1. 控制帧 ack 记录 histogram；
      2. audio/render handler 记录 chunk 延迟；
      3. placeholder `ragHit` flag.
    - 输出 `pino` 日志 embed trace/span id。
    - WHY: 满足 CTRL-03 观测要求。
  </action>
  <verify>
    <automated>OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318 pnpm --filter gateway test telemetry</automated>
  </verify>
  <done>
    - Gateway 启动时自动注册 tracer。
    - Metrics 成功发送到本地 collector（可在日志看到 export 成功）。
  </done>
</task>

<task type="auto">
  <name>Task 2: 设计 dashboard + 运维文档</name>
  <files>observability/dashboards/ctrl-latency.json, docs/observability/CTRL-03.md</files>
  <action>
    - 提供 Grafana JSON：
      - Panel1：Interrupt ACK p95/p99。
      - Panel2：ASR/TTS latency 双轴图。
      - Panel3：RAG hit rate (stacked bar per minute)。
    - 文档说明如何导入 dashboard、需要的 datasource、alert 阈值（ack>80ms or rag_hit<0.8）。
    - 记录如何运行 collector + `pnpm --filter gateway dev --telemetry`。
    - WHY: 指标无可视化就难以执行验证。
  </action>
  <verify>
    <automated>npm exec -- grafana-toolkit validate-dashboard observability/dashboards/ctrl-latency.json</automated>
  </verify>
  <done>
    - Dashboard JSON 通过 lint/validate。
    - 文档列出导入步骤 + Alert 规则。
  </done>
</task>

<task type="auto">
  <name>Task 3: k6 压测脚本覆盖 CTRL-03</name>
  <files>tests/load/k6-duplex.js</files>
  <action>
    - 使用 k6 ws API：
      1. 并行 100 VU，连接 `/audio` `/control`，发送 20ms Opus payload（可用 Buffer.from(random)）。
      2. 定期发 interrupt + 等待 ack，记录延迟 (Trend metric `ack_latency`).
      3. 模拟 render channel 收包以测量 agent/TTS placeholder latency。
    - 将 metrics 输出为 JSON Summary file（`--summary-export`），CI 可解析。
    - CLI 命令示例写入 README：`pnpm k6 run tests/load/k6-duplex.js --vus 100 --duration 1m --summary-export reports/ctrl03.json`。
    - WHY: 只有压测脚本才能反复验证指标。
  </action>
  <verify>
    <automated>K6_SUMMARY=reports/ctrl03.json pnpm k6 run tests/load/k6-duplex.js --vus 5 --duration 30s --summary-export reports/ctrl03.json</automated>
  </verify>
  <done>
    - k6 输出包含 ack latency p95 和 rag hit ratio。
    - README/Doc 记录如何解释结果。
  </done>
</task>

</tasks>

<verification>
- [ ] pnpm --filter gateway test telemetry && pnpm --filter gateway test
- [ ] grafana-toolkit validate-dashboard observability/dashboards/ctrl-latency.json
- [ ] pnpm k6 run tests/load/k6-duplex.js --vus 5 --duration 30s passes with ack_latency_p95 < 70ms
</verification>

<success_criteria>

- CTRL-03 指标具备采集、可视化、压测能力。
- Telemetry 代码默认启用且可配置。
- k6 脚本可一键运行并输出 JSON 供验证。
  </success_criteria>

<output>
完成后撰写 `.planning/phases/01-duplex-realtime-backbone/01-03-SUMMARY.md`。
</output>
